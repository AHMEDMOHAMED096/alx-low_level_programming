#include "dog.h"
/**
 * new_dog - creates a new dog
 * dog_t - Typedef for struct dog
 * @name: first variable
 * @age: second variable
 * @owner: third variable
 * Return: return NULL if function fails
 */

dog_t *new_dog(char *name, float age, char *owner)
{
	dog_t *dog = malloc(sizeof(new_dog);

	dog->name = name;
	dog->age = age;
	dog->owner = owner;
	dog = new_dog;
	if (dog == NULL)
	return (0);
}
